var categoryWindow = document.querySelector("#categories > .flex");

/*document.addEventListener('DOMContentLoaded', function() {
    function getVH(vh) {
        return (window.innerHeight * vh) / 100;
    }
    categoryWindow.addEventListener('wheel', function(event) {
        event.preventDefault();
        const scrollAmount = getVH(70);
        console.log(scrollAmount, window.innerHeight)
        if (event.deltaY > 0) {
            categoryWindow.scrollBy({ top: scrollAmount, behavior: 'auto' });
        } else {
            categoryWindow.scrollBy({ top: -scrollAmount, behavior: 'auto' });
        }
    });
});*/
